# Intellix admin panel — environment template
# Copy to .env when building locally. On Railway set it as a variable.

# Full base URL of the API, including the /v1 prefix
VITE_API_BASE="https://intellix-api.up.railway.app/v1"
